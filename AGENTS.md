# Agent Instructions

## Git Operations
Do not perform git operations (commits, pushes, branches, etc.). The user will handle all git workflows directly.

## Unit Tests
Do not write test code. Agents may create a testing plan or testing strategy, but should not write actual test code or implement unit tests.
